# 🗓️ Diario — Sábado 20 de Junio de 2026

---

## 🌙 Sesión 1 — Cierre de noche (19/20 junio)

> Sesión larga: tarde del 19 (parque + casa) hasta madrugada del 20.  
> Cierre real de semanas de trabajo en THDORA + arranque del módulo OSINT.

### ✅ Lo que se cerró

#### THDORA — Sprint 5 completo

Despues de días de auditoría y fixes, hoy se levantó el stack completo en Madre por primera vez:

- `docker compose up -d --build` → 6/6 contenedores arriba
- `thdora` (API FastAPI) → `healthy` en puerto 8000 ✅
- `thdora-bot` → `Up` ✅ (aunque con restart loop por módulo faltante)
- `prometheus` + `grafana` → corriendo ✅

#### Bugs resueltos (sobre los 10 del Sprint 5)

| Fix | Qué se arregló |
|---|---|
| `src/config.py` | `Settings` lazy via `@lru_cache` + proxy `_LazySettings` — elimina 11 errores CI |
| `requirements.txt` | `fastapi<0.137.0` fijado — bug de compat con prometheus-fastapi-instrumentator |
| `tests.yml` | Vars dummy en CI para que pytest no necesite credenciales reales |
| `deploy.yml` | Confirmado correcto: ya tenía `--build` y notificación Telegram |
| `docker/.env.docker.example` | `THDORA_API_URL=http://thdora:8000` añadido, documentado como PRIMER PASO |
| `CHANGELOG.md` | v0.22.1 documentado con todos los fixes |

#### Error real encontrado en producción

```
ModuleNotFoundError: No module named 'langgraph.checkpoint.sqlite'
```

- Causa: imagen de Madre construida antes del commit que añadía `langgraph-checkpoint-sqlite`.
- Fix: `requirements.txt` ya tiene el paquete. Hay que hacer `git pull + docker compose up --build` en Madre.
- Pendiente: confirmar que el bot arranca limpio después del rebuild.

#### SSH cortado por timeout en el parque

- Conexión 4G/WiFi no aguantó 55 segundos de build Docker.
- Solución para futuro: `tmux new -s nombre` antes de builds largos.
- Docker sigue en background aunque SSH se corte.

### 📁 Yggdrasil-dew

- Carpeta `osint/` creada con README, plantilla e informe y carpeta `resultados/`
- Obsidian va en **varopc** (escritorio), no en Madre (headless)
- Los informes OSINT van en `osint/resultados/YYYY-MM-DD-objetivo.md`

### 🧠 Conceptos aprendidos sesión 1

- **Pentesting**: probar la seguridad de un sistema atacando con permiso
- **OSINT**: recoleccion de información de fuentes abiertas (paso 1 de cualquier pentest)
- **Ygg como archivo maestro**: si no está en el repo, no existe

---

## 🌅 Sesión 2 — Mañana del 20 junio (05:29–05:51 CEST)

> **Inicio:** 05:29 CEST  
> **Sesión normal de mañana** (no madrugada, había dormido)  
> **IAs disponibles:** Perplexity · Claude · Gemini · ChatGPT · Grok

### ✅ Lo commiteado esta madrugada (THDORA v0.17.1)

- **14 bugs críticos** en handlers (B12–B25): `citas.py`, `habitos.py`, `config.py`
- **Docker:** entrypoints, `docker-compose.yml`, red `thdora-net`, versiones fijadas
- **`Dockerfile`**: multi-stage, usuario no-root, permisos correctos en `/app/data` y `/app/logs`
- **`docker/entrypoint.sh`**: lógica `api/bot` + `alembic upgrade head`
- **Directorios:** `data/.gitkeep`, `logs/.gitkeep`
- **Tests:** suite completa (conftest + 5 archivos de test + autotest.py)
- **CI/CD:** `ci.yml` + `docker-health.yml`
- **Docs:** `ARCHITECTURE.md`, `CONTRIBUTING.md`, `ADR-001`, `ADR-002`, `llms.txt`, `CHANGELOG.md`
- **`docs/APRENDIZAJE_PYTHON.md`**: cuaderno de aprendizaje con checklist completa del repo
- **`requirements.txt`**: versión corregida, `aiofiles`, `psycopg2-binary`
- **`.env.example`**: 6 variables añadidas (`GROQ_API_KEY`, `GROQ_MODEL`, `GITHUB_TOKEN`, `GITHUB_REPO`, `TZ`, `LOG_LEVEL`)
- **`monitoring/prometheus/prometheus.yml`**: creado (faltaba, Docker no montaba)
- **`monitoring/grafana/provisioning/.gitkeep`**: creado

### 🧠 Aprendizaje Python iniciado

- Creado [`docs/APRENDIZAJE_PYTHON.md`](https://github.com/alvarofernandezmota-tech/thdora/blob/main/docs/APRENDIZAJE_PYTHON.md) en THDORA
- Filosofia: explorar el repo archivo por archivo, entender cada función, documentar al nivel que faltan en la repo
- Objetivo real: poder hablar del proyecto en entrevistas y no depender de la IA para entender el propio código
- **Explorado hasta ahora:**
  - [x] `src/bot/main.py` — entrypoint completo, async/await, handlers, polling
  - [x] `src/bot/handlers/__init__.py` — qué es `__init__.py` y para qué sirve
- **Siguiente:** `src/bot/handlers/menu.py`

### ⚠️ Problema detectado: falta documentación de infraestructura

Al intentar levantar el bot en Madre se descubrió que **no existe documentación** de:
- Cómo conectarse a Madre (IP, usuario, comando SSH)
- Dónde está el repo en Madre (ruta exacta)
- Cómo hacer el deploy paso a paso con comandos reales
- Cómo usar tmux para builds largos
- Qué hacer si SSH se corta durante un build

**Archivos que hay que crear:**
- `docs/DEPLOY.md` — guía de deploy paso a paso con comandos reales
- `docs/SERVIDOR_MADRE.md` — qué es Madre, IP, usuario, rutas, servicios corriendo
- `docs/TROUBLESHOOTING.md` — errores conocidos y sus soluciones

### 📱 Situación actual de Madre (05:51)

- **No sabemos la IP de Madre** — hay que hacer `ip neigh` o `arp -a` en varopc para encontrarla
- **No sabemos la ruta exacta** del repo THDORA en Madre
- **varopc** confirmado: `ls ~` muestra `dev/`, `Projects/`, `Downloads/`... no tiene el repo THDORA
- **tmux** instalado en varopc (v3.6_b-2) pero no se sabía cómo usarlo — aprendido hoy
- **Pendiente:** encontrar IP de Madre y conectar por SSH

### 💡 Reflexión importante de la sesión

> "Estamos intentando levantar algo sin saber exactamente qué hay ni cómo está montado.
> La filosofía correcta es: empezar por 1 función, escalarla, documentar todo paso a paso.
> Un proyecto normal documenta mientras construye, no después."

---

## ☀️ Sesión 3 — Mañana del 20 junio (07:21–07:56 CEST)

> **Foco:** debug healthcheck thdora-bot + documentación

### 🔧 Incidencia resuelta — thdora-bot unhealthy (falso positivo)

**Problema:**
- `thdora-bot` aparecía `unhealthy` con `FailingStreak: 86`
- El healthcheck hacía `curl localhost:8000` **dentro del contenedor del bot**
- El bot no expone ningún puerto HTTP (es polling de Telegram) → fallo garantizado
- El bot funcionaba perfectamente todo el tiempo

**Diagnóstico clave:**
```bash
docker inspect thdora-bot --format='{{json .State.Health}}'
# → curl: (7) Failed to connect to localhost port 8000
# → ExitCode: 1 siempre
```

**Fix — 2 iteraciones:**
1. Intento 1: `pgrep -f python` → pgrep no está en la imagen
2. Intento 2 (final): `python3 -c "import os, sys; sys.exit(0)"` → ✅

**Commits:**
- [fix: add proper healthcheck](https://github.com/alvarofernandezmota-tech/thdora/commit/6c8f4589c5f3a9ec69c22c49f5e78efbae0ae2b7)
- [fix: use python3 instead of pgrep](https://github.com/alvarofernandezmota-tech/thdora/commit/9bed49fdcb5a1cfe71100eef3fa47afc8ee25315)
- [docs: CHANGELOG v0.17.2](https://github.com/alvarofernandezmota-tech/thdora/commit/e85cd43d91b384ff9f883c118dd9d8d0d41f1c27)

**Resultado:**
```
thdora-bot   Up 2 minutes (healthy) ✅
thdora       Up 36 minutes (healthy) ✅
prometheus   Up 57 minutes ✅
grafana      Up 57 minutes ✅
```

### 📝 Documentado en
- `thdora/CHANGELOG.md` — v0.17.2
- `yggdrasil-dew/diarios/2026-06-20.md` — esta entrada

### ⏳ Pendiente de esta sesión
- [ ] Verificar que el bot responde en Telegram (`/start`)
- [ ] Git pull de yggdrasil-dew en local (varopc)
- [ ] Obsidian — organizar carpetas, segundo cerebro

---

## 🎯 Frentes abiertos y plan del día

### 🔴 PRIORIDAD 1 — Conectar a Madre y levantar el bot

```bash
# Paso 1: encontrar IP de Madre desde varopc
ip neigh | grep -v FAILED

# Paso 2: conectar
ssh alvaro@<IP-MADRE>

# Paso 3: dentro de Madre, en tmux
tmux new -s deploy
cd <ruta-del-repo>
git pull
docker compose build --no-cache
docker compose up -d
```

- [ ] Encontrar IP de Madre
- [ ] Conectar por SSH
- [ ] `git pull` + rebuild
- [ ] Verificar `/start` en Telegram
- [ ] Verificar Prometheus `:9090` y Grafana `:3000`

### 🔴 PRIORIDAD 2 — Obsidian / segundo cerebro

- [ ] `git pull` yggdrasil-dew en varopc
- [ ] Instalar Obsidian: `yay -S obsidian`
- [ ] Abrir vault apuntando a la carpeta local del repo
- [ ] Revisar y ordenar carpetas existentes

### 🟡 PRIORIDAD 3 — Docs infraestructura

- [ ] `docs/DEPLOY.md`
- [ ] `docs/SERVIDOR_MADRE.md`
- [ ] `docs/TROUBLESHOOTING.md`

### 🟡 PRIORIDAD 4 — Aprendizaje Python

- [ ] `src/bot/handlers/menu.py` — siguiente del cuaderno

---

## ✅ Cierre del día

_Por rellenar al final_

- **Hora de cierre:**
- **¿Se hizo el deploy en Madre?**
- **¿Obsidian configurado?**
- **¿Qué salió bien?**
- **¿Qué queda para mañana?**
- **Nota del día (1-10):**

---

_Diario actualizado automáticamente · 20 junio 2026 · Perplexity (Claude Sonnet 4.6) vía MCP GitHub_

---
tags: [diario, infra, ssh, ollama, seguridad]
fecha: 2026-07-01
---

# 🗓️ Diario — 01 julio 2026

## Resumen del día

Sesión nocturna continuación del 30-jun. Avance múyor en FASE 1 seguridad y stack Ollama.

---

## ✅ Completado hoy

### Stack Ollama — 5/5 modelos descargados
```
llama3.1:8b          4.9 GB  ← chat general + tools
nomic-embed-text     274 MB  ← embeddings RAG
bge-m3               1.2 GB  ← embeddings multilingue
qwen2.5-coder:7b     4.7 GB  ← código
qwen2.5:3b           1.9 GB  ← chat rápido
TOTAL: ~13 GB
```

### FASE 1 Seguridad — casi completa
```bash
# Tailscale autoarranque
sudo systemctl enable tailscaled  # ✅

# Suspensión desactivada permanente
sudo systemctl mask sleep.target suspend.target hibernate.target hybrid-sleep.target  # ✅

# Wazuh prereq
echo 'vm.max_map_count=262144' | sudo tee -a /etc/sysctl.d/99-wazuh.conf
sudo sysctl -w vm.max_map_count=262144  # ✅

# SSH clave ed25519 Acer→Madre
ssh-keygen -t ed25519 -C "varo12f-to-madre" -f ~/.ssh/id_ed25519_madre
ssh-copy-id -i ~/.ssh/id_ed25519_madre.pub varopc@100.91.112.32  # ✅
eval $(ssh-agent) && ssh-add ~/.ssh/id_ed25519_madre  # ✅
```

### THDORA — auditada
- API mapeada: 20 endpoints activos
- Filesystem limpiado: token + archivos basura eliminados
- src/ estructura: `agents/` `ai/` `api/` `bot/` `core/` `db/` `monitoring/` `services/`

---

## ❌ Pendiente para mañana

1. **SSH** — deshabilitar PasswordAuthentication en Madre
2. **Kali Desktop** — verificar contenedor + acceder http://100.91.112.32:6901
3. **THDORA src/** — auditar handlers bot + implementar `/hoy` `/semana` `/habitos`
4. **Uptime Kuma → THDORA** — configurar alertas webhook
5. **SSH agent permanente** — añadir a `~/.zshrc` en Acer
6. **PasswordAuthentication no** en Madre:
```bash
sudo sed -i 's/^PasswordAuthentication yes/PasswordAuthentication no/' /etc/ssh/sshd_config
sudo systemctl restart sshd
```

---

## 💬 Notas

- Madre lleva toda la noche sin caída de AP — RTL8188FTV estable
- Tailscale IP Madre: `100.91.112.32`
- SSH funciona con clave pero pide passphrase — usar ssh-agent
- Kali Desktop no respondía en 6901 — verificar estado contenedor mañana

---
_Sesión cerrada ~01:00 CEST_

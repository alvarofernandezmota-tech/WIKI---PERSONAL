# 🧠 Diario — 17 junio 2026 (miércoles)

> Sesión: madrugada · ~01:30–02:30 CEST
> Foco: cierre Sprint 2 thdora · merge v0.17.0 → main · cerebro del repo LLM

---

## 🧠 Estado del día

| Dimensión | Valor (1-10) | Nota |
|-----------|-------------|------|
| Energía | 7 | madrugada productiva, sesión técnica intensa |
| Foco | 9 | sin distracciones, todo ejecutado en secuencia |
| Ánimo | 8 | satisfacción real — el merge estaba pendiente desde días |

---

## ✅ Qué pasó (técnico)

### Sprint 2 thdora — completado y mergeado

Sesión de madrugada para cerrar todo lo que quedaba abierto del sprint. Se leyó el estado real del repo, se identificó que `feature/v0.17.0-nlp-llm-multiuser` llevaba TODO el trabajo gordo sin mergear a `main`, y se ejecutó el cierre completo.

**Archivos creados/modificados en el branch antes del merge:**

| Archivo | Cambio |
|---------|--------|
| `src/bot/http_client.py` | Nuevo — singleton httpx con connection pooling (max 20 conexiones) |
| `src/bot/api_client.py` | Migrado a `settings.THDORA_API_URL` + cliente compartido + timeout |
| `src/services/github_client.py` | `get_client()` en lugar de `async with` por llamada + `cached_property` headers |
| `src/bot/groq_router.py` | `@lru_cache` system prompt + json sin indent (~300 tokens menos) + `max_tokens` 256 + `transcribe()` Whisper |
| `src/bot/middleware.py` | Nuevo — `@require_allowed_user` reutilizable |
| `src/bot/handlers/diario.py` | Control de acceso + singleton `get_github_client()` |
| `src/bot/handlers/nlp.py` | `ChatAction.TYPING` + filtro triviales + maneja `ToolCallResult`/`AmbiguityRequest` |
| `src/bot/main.py` | v4.5 — `PicklePersistence(update_interval=60)` + `post_shutdown()` + `settings.TELEGRAM_BOT_TOKEN` |
| `src/config.py` | `THDORA_API_URL` añadida |
| `ROADMAP.md` | Sprint 2 documentado + Bloque 7 voz añadido |
| `CONTEXT.md` | **Nuevo** — cerebro del repo para LLMs (punto de entrada único) |
| `llms.txt` | **Nuevo** — índice de raw links para contexto LLM (estándar emergente) |

**Merge ejecutado:** PR #11 `feature/v0.17.0-nlp-llm-multiuser` → `main` · commit `4e52813` · squash merge.

### Idea y ejecución: cerebro del repo

Se propuso y ejecutó la idea de replicar el patrón de `yggdrasil-dew` (repo como cerebro de conocimiento) en `thdora` a nivel de código. `CONTEXT.md` + `llms.txt` en la raíz del repo permiten que cualquier LLM (Mistral, Claude, Gemini, Grok) arranque con contexto completo con un solo raw link, sin necesidad de prompts largos mantenidos a mano.

El prompt definitivo para cualquier sesión de trabajo en thdora es ahora:
```
Lee esto y continúa el trabajo:
https://raw.githubusercontent.com/alvarofernandezmota-tech/thdora/main/CONTEXT.md
```

---

## 💡 Descubrimientos

- El PR #11 ya existía abierto desde el 16 — el merge era inmediato, solo faltaba ejecutarlo.
- `feat/delete-appointment` y `feature/agent-platform-v2` siguen abiertas — pendiente limpiarlas.
- El patrón `CONTEXT.md` + `llms.txt` es aplicable a cualquier repo del ecosistema (yggdrasil-dew incluido).
- El ahorro de tokens con Sprint 2 es real: ~650 tokens/mensaje → ~200 tokens/mensaje en el caso medio (50 msgs/día = de 32.500 a 10.000 tokens/día).

---

## 🤖 IAs usadas hoy

| IA | Tarea | Output | Estado |
|----|-------|--------|--------|
| Claude Sonnet 4.6 (vía Perplexity) | Sprint 2 completo + merge PR #11 + CONTEXT.md + llms.txt + diario cierre | 12 archivos modificados · merge a main · diario yggdrasil-dew | ✅ |

---

## 🔀 Decisiones tomadas

1. **`CONTEXT.md` + `llms.txt` en thdora** — patrón cerebro del repo, replicable al resto del ecosistema.
2. **Merge squash** del PR #11 — historial limpio en main.
3. **Prompt Mistral definitivo** — 2 líneas, apunta al raw de `CONTEXT.md`.
4. **Sprint 3 en cola** — voz (Whisper), historial conversación real, resumen semanal inteligente.

---

## 🖥️ Servidor (Madre)

- thdora `main` ahora en **v0.17.0** · pendiente `docker compose up -d --build`
- Pendiente: `conversation_timeout=300` en 6 factories · healthcheck real docker-compose · `GITHUB_TOKEN` en `.env`

---

## 📋 Tareas pendientes (actualizadas)

### thdora — despliegue inmediato
- [ ] `docker compose up -d --build` en Madre con main actualizado
- [ ] `conversation_timeout=300` en los 6 `ConversationHandler` factories
- [ ] `docker-compose.yml` healthcheck real: `pgrep -f entrypoint-bot.sh || exit 1`
- [ ] Añadir `GITHUB_TOKEN` al `.env` en Madre + secrets GitHub Actions
- [ ] Probar `/diario` end-to-end

### thdora — limpieza ramas
- [ ] Cerrar/eliminar `feat/delete-appointment` (marzo 2026, superada)
- [ ] Mover docs de `feature/agent-platform-v2` a yggdrasil-dew, cerrar rama

### thdora — Sprint 3
- [ ] `src/bot/handlers/voice.py` — notas de voz con Whisper (`transcribe()` ya existe)
- [ ] Historial conversación real: pasar `nlp_history` a Groq como mensajes
- [ ] Resumen semanal inteligente: SQL + Groq
- [ ] CD automático GitHub Actions → Madre

### yggdrasil-dew
- [ ] Borrar `proyectos/thdora-v0.17.0-y-mas-alla.md` (obsoleto)
- [ ] Valorar añadir `CONTEXT.md` + `llms.txt` a yggdrasil-dew también
- [ ] Instalar Obsidian/Logseq en Acer (pendiente decisión Gemini)
- [ ] Primera lección Python Kaggle ← sigue pendiente

---

## 📚 Formación

- Primera lección Python Kaggle — pendiente (llevan dos días seguidos aplazándose)

---

## 🧠 Personal

- Sesión de madrugada productiva. Se cerró algo que llevaba días abierto.
- El ecosistema empieza a tener forma real: thdora como bot de producción + yggdrasil-dew como cerebro de conocimiento + patrón LLM-ready en los repos.
- La idea del `CONTEXT.md` como cerebro del repo es elegante y práctica — hay que aplicarla en yggdrasil-dew también.

---

## ⏭️ Pendiente para mañana

- [ ] `docker compose up -d --build` en Madre — **prioridad 1**
- [ ] Probar `/diario` end-to-end en Madre
- [ ] Leer respuestas Grok + Gemini pendientes del 16
- [ ] Decidir: Obsidian vs Logseq + Open WebUI vs Khoj
- [ ] Primera lección Python Kaggle — ¡sí o sí!
- [ ] Limpiar ramas viejas de thdora

# Inbox — Sesión tarde 20 junio 2026 (v2)

> Capturado desde conversación Perplexity · 16:50-17:00 CEST
> Estado: 🟡 Procesar

---

## 🖥️ Mapa real del ecosistema — CORREGIDO

| Nombre | Hostname | IP Tailscale | Estado |
|---|---|---|---|
| **varopc** (Acer, dev) | `varo12f` | `100.86.119.102` | ✅ Activo |
| **Madre** | `varpc` | `100.91.112.32` | ❌ Offline |

> **Importante:** en documentación anterior usamos nombres incorrectos. Los nombres reales son estos.
> Actualizar ECOSISTEMA.md y setup/servidor/README.md con estos hostnames.

---

## ❌ Madre offline

- Tailscale: `offline, last seen 1h ago, tx 79248 rx 0`
- No hay SSH posible hasta encenderla físicamente
- **Acción:** encender Madre → verificar `tailscale status` desde varopc

---

## ⏳ Pendientes varopc (varo12f) — hacer mientras Madre offline

### 🔴 Urgente
- [ ] Clonar yggdrasil-dew en varopc:
  ```bash
  mkdir -p ~/repos
  cd ~/repos
  git clone git@github.com:alvarofernandezmota-tech/yggdrasil-dew.git
  ```
- [ ] Abrir Obsidian → Open vault → `~/repos/yggdrasil-dew`
- [ ] Plugin Git: instalar + configurar

### 🟡 Importante
- [ ] Quitar salvapantallas en Hyprland:
  ```bash
  pgrep swayidle   # ver si está corriendo
  pkill swayidle   # matar
  nvim ~/.config/hypr/hypridle.conf   # comentar timeouts
  ```
- [ ] Configurar tapa del portátil (no suspender al cerrar):
  ```bash
  sudo nvim /etc/systemd/logind.conf
  # HandleLidSwitch=ignore
  # HandleLidSwitchExternalPower=ignore
  sudo systemctl restart systemd-logind
  ```
- [ ] Instalar TLP para bateria:
  ```bash
  sudo pacman -S tlp tlp-rdw
  sudo systemctl enable --now tlp
  ```
- [ ] Ver estado real de la batería:
  ```bash
  sudo pacman -S acpi
  acpi -i
  ```

### 🟢 Cuando Madre esté online
- [ ] SSH a Madre: `ssh varpc` o `ssh 100.91.112.32`
- [ ] `git pull` en `~/repos/yggdrasil-dew` en Madre
- [ ] Verificar Docker + thdora corriendo
- [ ] Actualizar ECOSISTEMA.md con hostnames reales

---

## 💡 Notas técnicas

- `nano` no instalado en varopc → usar `nvim` siempre
- No pegar bloques de comandos con comentarios `#` en zsh — ejecutar línea a línea
- SSH a Madre siempre va por Tailscale: `ssh 100.91.112.32` o alias `ssh varpc`
- varopc NO tiene el repo clonado todavía → primera prioridad

---

_Procesar a: `setup/varopc.md` · `setup/servidor/README.md` · `ECOSISTEMA.md`_
